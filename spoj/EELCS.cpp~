/* bhupkas */

using namespace std;

#include "bits/stdc++.h"

int T[20][20];

int main()
{
	int n,m;
	string A,B;
	cin >> A >> B;
	n = A.size();
	m = B.size();
	for(int i = 0 ; i <= n ; ++i)	T[i][0] = 0;
	for(int j = 0 ; j <= m ; ++j)	T[0][j] = 0;
	
	return 0;
}
